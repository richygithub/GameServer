using SharedLib;
using System;
using System.Collections.Generic;
using System.Text;

using UseLibuv;
namespace Client
{

    /*
    class ClientDispatcher  
    {

        InputStream _istream = new InputStream();
        public void Process(Channel c, List<Packet> packets)
        {
            //throw new NotImplementedException();
            foreach(var packet in packets)
            {
                _istream.SetUp(packet.body, packet.len);

                //NetClient.Instance.
                //packet.seqId

            }
        }
    }
    */
}
